# strings can be enclosed in double or single quotes

print("Python's strings are easy to use")

# concatenate strings to make longer ones using +

print('hello'+' world')

# you can store strings in variables, which makes contatenation make more sense

greeting = 'hello'
name = 'bruce'

print(greeting + ' ' + name)

# use the input function to read in a name from the keyboard

greeting = 'hello'
name = input('Please enter your name ')

print(greeting + ' ' + name)

# the imput function is called first and its value is assigned to the variable name