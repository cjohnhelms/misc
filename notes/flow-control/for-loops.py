# for loops work by iterating over some set of values
# it assigns each of the values, one by one, to one or more variables
# executes a block of code once for each value
# an iterable object is anything that can be iterated over, like a sequence

parrot = 'Norwegian Blue'

for character in parrot:
    print(character)